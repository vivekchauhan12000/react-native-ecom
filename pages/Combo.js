import React from 'react'
import { View, Text } from 'react-native'

const Combo = ({ navigation }) => {
  return (
    <View>
      <Text>Combo</Text>
    </View>
  )
}

export default Combo
